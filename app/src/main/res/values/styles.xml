<resources>

    <!-- Base application theme. -->
    <style name="AppDarkTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimaryAccentGradient">@color/colorPrimaryAccentGradient</item>
        <item name="colorBackground">@color/colorDark</item>
        <item name="colorBackgroundElement">@color/colorDeepDark</item>
        <item name="colorBackgroundContrast">@color/colorContrastDark</item>
        <item name="colorText">@color/colorWhite</item>
        <item name="colorTextContrast">@color/colorLightGrey</item>
        <item name="colorError">@color/colorError</item>
        <item name="android:windowBackground">@color/colorDark</item>
        <item name="android:navigationBarColor">@color/colorDark</item>

        <!-- Font -->
        <item name="android:fontFamily">@font/whitney</item>
    </style>

    <!-- The launcher theme. It sets the main window background to the launch_screen drawable -->
    <style name="AppDarkTheme.Launcher">
        <item name="android:windowBackground">@drawable/launch_screen</item>
    </style>

    <style name="AppDarkTheme.SlidrActivityTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="DarkThemeDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/colorDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/colorWhite</item>
    </style>

</resources>
